import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'سلة التسوق | Bin Siddiq Fabric' };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
